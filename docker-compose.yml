version: "3.8"

services:
  postgres:
    image: postgres:16-alpine
    container_name: DBP_PROYECTO
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: DBP_PROYECTO
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
